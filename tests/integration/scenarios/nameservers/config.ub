# The domain input is supplied through the UB_VAR_domain environment variable.

configurations: { namecheap: { default: {} } }

state: { @backend: local, path: '.unobin/state', encryption: { @key-source: noop } }
